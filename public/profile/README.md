# Profile image

The approved headshot is currently stored at `public/profile/quan-tran-headshot.webp`. To replace it later, keep the same filename or update `profile.profileImage` in `content/site.ts`.

Do not replace it with an AI-generated portrait.
